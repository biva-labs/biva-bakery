{
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "bun src/index.ts",
    "healthcheckPath": "/ping",
    "healthcheckTimeout": 100
  }
}